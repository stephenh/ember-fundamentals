import ifthen from 'emberli/utils/ifthen';
import { module, test } from 'qunit';

module('Unit | Utility | ifthen');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifthen();
  assert.ok(result);
});
